---
interface Props {
  title: string;
  date: Date;
  description?: string;
  tags?: string[];
  growthStage?: 'draft' | 'published';
  href: string;
}

const { title, date, description, tags = [], growthStage = 'published', href } = Astro.props;

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<article class="post-card">
  <a href={href} class="post-card__link">
    <div class="post-card__meta">
      <time datetime={date.toISOString()}>{formattedDate}</time>
      {growthStage === 'draft' && <span class="badge badge--draft">Draft</span>}
    </div>
    <h3 class="post-card__title">{title}</h3>
    {description && <p class="post-card__description">{description}</p>}
    {tags.length > 0 && (
      <div class="post-card__tags">
        {tags.map((tag) => (
          <span class="tag">{tag}</span>
        ))}
      </div>
    )}
  </a>
</article>

<style>
  .post-card {
    border-bottom: 1px solid var(--color-border-subtle);
    padding: var(--space-5) 0;
  }

  .post-card:first-child {
    padding-top: 0;
  }

  .post-card:last-child {
    border-bottom: none;
  }

  .post-card__link {
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .post-card__link:hover .post-card__title {
    color: var(--color-accent);
  }

  .post-card__meta {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    margin-bottom: var(--space-1);
  }

  .post-card__meta time {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .post-card__title {
    font-family: var(--font-serif);
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-text-heading);
    line-height: var(--leading-snug);
    margin-bottom: var(--space-1);
    transition: color var(--transition-fast);
  }

  .post-card__description {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: var(--leading-normal);
    margin-bottom: var(--space-2);
  }

  .post-card__tags {
    display: flex;
    gap: var(--space-2);
    flex-wrap: wrap;
  }
</style>
